{
  "name": "cls-merge",
  "version": "1.0.1",
  "description": "class merging npm package",
  "main": "dist/index.ts",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "jest"
  },
  "repository": {
    "type": "github",
    "url": "https://github.com/sogabe-tatsuki/cls-merge"
  },
  "homepage": "https://github.com/sogabe-tatsuki/cls-merge#readme",
  "author": "Tatsuki Sogabe",
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.3.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.10",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1"
  },
  "jest": {
    "moduleFileExtensions": [
      "ts",
      "js"
    ],
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "globals": {
      "ts-jest": {
        "tsConfig": "tsconfig.json"
      }
    },
    "testMatch": [
      "**/tests/**/*.test.ts"
    ]
  }
}
